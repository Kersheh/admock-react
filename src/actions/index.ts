import { openClose, setCategory } from './sidebar'; // Example to remove in favour of local management

export {
  openClose, // Example to remove in favour of local management
  setCategory // Example to remove in favour of local management
};
